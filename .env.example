# ===== SINGLE-USER MODE (Default, for local development) =====
# Guild Wars 2 API Configuration
# Get your API key from: https://account.arena.net/applications
GW2_API_KEY=your-api-key-here

# Polling Configuration (in seconds)
# Dashboard refresh interval (5-300 seconds, default: 60)
POLLING_DASHBOARD_INTERVAL=60
# Interactive maps refresh interval (5-300 seconds, default: 30)
POLLING_MAPS_INTERVAL=30


# ===== MULTI-TENANT MODE (For VPS deployment) =====
# Uncomment and configure these for multi-tenant deployment

# Database Configuration
# DATABASE_URL=postgresql://gw2api_user:password@localhost/gw2api

# Security Keys (Generate with: python3 -c "import secrets; print(secrets.token_hex(32))")
# JWT_SECRET_KEY=<generate-with-secrets-token-hex-32>
# SECRET_KEY=<generate-with-secrets-token-hex-32>

# API Key Encryption (Generate with: python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())")
# API_KEY_ENCRYPTION_KEY=<generate-with-fernet-generate-key>

# Flask Configuration
# FLASK_ENV=production
# FLASK_DEBUG=False

# Rate Limiting
# RATE_LIMIT_ENABLED=true
# RATE_LIMIT_PER_MINUTE=60

# Server Configuration
# HOST=0.0.0.0
# PORT=5555

# Application Prefix (for nginx path-based routing)
# APP_PREFIX=/gw2
